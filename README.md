# README

- add text

- 브랜치 확인

  - git branch

- 브랜치 생성

  - git branch {branch_name}

- 브랜치 전환

  - git checkout {branch_name}
    - `-b` : 생성 하면서 전환
  - git switch {branch_name}
    - `-c` : 생성 하면서 전환

- 브랜치 삭제
  - git branch -d {banch_name}
